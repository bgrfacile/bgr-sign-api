spring:
  config:
    activate:
      on-profile: prod
#    import: "optional:file:configuration/prod.properties"
  datasource:
#    url: jdbc:postgresql://${DB_HOST:prod-db-server}:5432/bgr_sign_db
#    username: ${DB_USER}
#    password: ${DB_PASSWORD}
    url: jdbc:postgresql://postgres:5432/bgr_sign_db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
#    hikari:
#      maximum-pool-size: 10
#      connection-timeout: 30000
#      idle-timeout: 600000
#      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration
    enabled: true
    validate-on-migrate: true

logging:
  level:
    root: warn
    org.flywaydb: info

app:
#  jwtSecret: ${JWT_SECRET}
  jwtSecret: eLdw8vR8Iu8YkQor3rR4g1s1qUaNoJJemFc5kqNLKYs=
  jwtExpirationInMs: 604800000

debug: false

server:
  port: 8080